{"area": "", "auditor": "audit", "body": "## Resolution\n`cfg` was nil when `rootCmd.AddCommand(...)` constructed the subcommands\n(config/encrypt/decrypt captured the nil pointer); `PersistentPreRunE` then loaded\nconfig into the package var but the subcommands kept the stale nil. `ConfigCmd`\ndereferenced the typed-nil pointer at construction (`config.LogFilePath`), so\n`a <anything>` SIGSEGV'd before running. Verified by reproduction. Fix: allocate\n`cfg = &cmd.Config{}` up front and have `loadConfig` mutate it in place\n(`*cfg = *loaded`) so the captured pointer observes loaded values.\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-001", "resolved": "2026-06-26", "severity": "", "status": "fixed", "title": "CLI panics at startup on every invocation"}
{"area": "", "auditor": "audit", "body": "## Resolution\n`PersistentPreRunE` calls `loadConfig`, and the old `LoadConfig` errored when\nthe file was absent, so `a config` (the only way to create a config) failed in its\nown PreRun. Fix: `InitConfigPaths` now materializes a default `config.yaml` (0600)\non first run, and `LoadConfig` returns a defaulted config when the file is missing.\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-002", "resolved": "2026-06-26", "severity": "", "status": "fixed", "title": "Bootstrap chicken-and-egg: new user can never create config"}
{"area": "", "auditor": "audit", "body": "## Resolution\n`tryDecrypt` validated `[\"-d\",\"-i\",keyPath,\"-o\",output,input]` by treating\nindices 0/2/4 as flags, but index 2 is `keyPath` and 4 is `output`, so it always\nreturned \"unexpected flag in age arguments\". Decryption could never succeed. Fix:\nremoved the broken security-theater loop (args are built from literal flags\ninternally), kept the meaningful keyPath/output validations.\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-003", "resolved": "2026-06-26", "severity": "", "status": "fixed", "title": "Decryption always fails (broken arg-validation loop)"}
{"area": "", "auditor": "audit", "body": "## Resolution\n`buildAgeArgs` passed every recipient via `age -r`, but recipients are SSH\npublic-key file paths (per README and `default_recipients`). `-r` expects a key\nstring; file paths require `-R`. Only GitHub-fetched key strings worked. Fix:\nclassify each recipient — existing file → `-R`, otherwise `-r`.\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-004", "resolved": "2026-06-26", "severity": "", "status": "fixed", "title": "Encrypting to public-key files always fails"}
{"area": "", "auditor": "audit", "body": "## Resolution\n`ConfigCmd` unconditionally overwrote every field from flag values, so\n`a config --github-user x` reset ssh_key_path, recipients, and ttl to empty/zero.\nFix: only assign fields whose flag was `Changed()`. Also returned an error (instead\nof silent `nil`) on a bad type assertion, and removed the now-redundant flag-default\nbranch.\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-005", "resolved": "2026-06-26", "severity": "", "status": "fixed", "title": "config command wipes unset fields"}
{"area": "", "auditor": "audit", "body": "## Resolution\nage writes output honoring umask (observed 0664), leaving decrypted secrets\nreadable by group/other. Fix: `tryDecrypt` chmods the output to 0600 after a\nsuccessful decrypt.\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-006", "resolved": "2026-06-26", "severity": "", "status": "fixed", "title": "Decrypted plaintext left world/group-readable"}
{"area": "", "auditor": "audit", "body": "## Resolution\nIt recomputed `expectedDir` from `os.UserConfigDir()` and rejected any\ncfgFile not under it via `strings.HasPrefix` — ignoring its own parameter contract,\nusing a weak prefix check (`/.config/a-evil` passes), and making the function\nuntestable. cfgFile is supplied by `InitConfigPaths` (trusted), so the check was\nremoved; the existing `#nosec G304` covers the read.\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-007", "resolved": "2026-06-26", "severity": "", "status": "fixed", "title": "LoadConfig path validation was wrong and broke legitimate use"}
{"area": "", "auditor": "audit", "body": "## Resolution\n`generateSSHKeyPair` hardcoded `id_rsa`, so the second call in\n`TestEncryptDecrypt_WrongKey` failed (ssh-keygen won't overwrite), returned an empty\npath, and decrypt fell back to scanning the developer's real `~/.ssh`. Fix: place\neach keypair in its own `os.MkdirTemp` subdirectory (filename stays `id_rsa`, which\ndecryption requires).\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-008", "resolved": "2026-06-26", "severity": "", "status": "fixed", "title": "Test helper key-name collision scanned the real ~/.ssh"}
{"area": "", "auditor": "audit", "body": "## Resolution\nExamples were `a encrypt -o out.txt input.txt`, but the positional arg is\nignored and `-i/--input` is required, so the documented command errors with \"input\nfile is required\". Fixed to `-i input.txt -o out.txt` (and the decrypt analogue).\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-009", "resolved": "2026-06-26", "severity": "", "status": "fixed", "title": "README encrypt/decrypt examples use wrong invocation"}
{"area": "", "auditor": "audit", "body": "## Resolution\nLinked `[LICENSE](LICENSE)` but the file is `LICENSE.md`. Fixed the target.\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-010", "resolved": "2026-06-26", "severity": "", "status": "fixed", "title": "README LICENSE link is broken"}
{"area": "", "auditor": "audit", "body": "## Resolution\nREADME said \"Go (1.21+)\" while go.mod requires `go 1.26.4`. Updated to 1.26+.\n\n### Pass 2 — 2026-06-26\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-011", "resolved": "2026-06-26", "severity": "", "status": "fixed", "title": "README states wrong Go version"}
{"area": "", "auditor": "audit", "body": "## Resolution\n`CacheTTLMinutes` and the `CacheDir` from `InitConfigPaths` were never read;\nGitHub key fetches hit the network every time. Implemented the intended caching:\n`fetchGitHubKeys` now stores the `.keys` response at `<CacheDir>/<user>.keys` (0600)\nand reuses it while younger than `CacheTTLMinutes`. `CacheDir` is threaded into\n`Config` (yaml:\"-\") via `a.go`, the first-run config now defaults `cache_ttl_minutes`\nto 120, and `0` disables caching. Covered by new tests in cmd/encrypt_test.go and\nverified end-to-end (cache file written, second run served from cache).\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-012", "resolved": "2026-06-26", "severity": "", "status": "fixed", "title": "CacheTTLMinutes was dead configuration"}
{"area": "", "auditor": "audit", "body": "## Resolution\nRemoved the `.txt`/`.out` output allow-list from `buildAgeArgs` and\n`tryDecrypt` so arbitrary output filenames (e.g. `foo.tar.age`) are accepted. The\nrestriction provided no security value (output name is caller-chosen). Covered by\n`TestBuildAgeArgs`.\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-013", "resolved": "2026-06-26", "severity": "", "status": "fixed", "title": "Output restricted to .txt/.out extensions"}
{"area": "", "auditor": "audit", "body": "## Resolution\nRemoved the brittle filename-suffix check in `tryDecrypt`, which rejected\nvalid keys with non-standard names (e.g. `~/.ssh/work_ed25519`) supplied via\n`--ssh-key`. age itself validates the key and unusable keys simply fail that attempt.\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-014", "resolved": "2026-06-26", "severity": "", "status": "fixed", "title": "Decryption key restricted to id_rsa/id_ed25519 suffix"}
{"area": "", "auditor": "audit", "body": "## Resolution\nRemoved `ageBin := \"age\"; if ageBin != \"age\" { ... }` (never true) and inlined\nthe `\"age\"` literal, matching the cleanup already done in `tryDecrypt`.\n\n### Pass 3 — 2026-06-26\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-015", "resolved": "2026-06-26", "severity": "", "status": "fixed", "title": "Dead binary-name guard in runAgeEncrypt"}
{"area": "", "auditor": "audit", "body": "## Resolution\n`a completion powershell` exited 0 with no output and no error (the `switch`\nhad no default), and completion-generation errors were discarded (`_ =`). Converted\n`Run` to `RunE`, returning an error for unsupported shells and propagating generator\nerrors; added `ValidArgs`. Covered by cmd/completion_test.go.\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-016", "resolved": "2026-06-26", "severity": "", "status": "fixed", "title": "completion command silently no-ops on an unknown shell"}
{"area": "", "auditor": "audit", "body": "## Resolution\nThe check `info.Mode().Perm() != 0o600` rejected a more-restrictive `0400`\nconfig, a footgun for users hardening their config. Replaced with `perm&0o077 != 0`\n(reject only group/other access). Covered by `TestLoadConfig_AcceptsStricterPerms`\nand `TestLoadConfig_RejectsGroupOtherPerms`.\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-017", "resolved": "2026-06-26", "severity": "", "status": "fixed", "title": "LoadConfig required exactly 0600, rejecting stricter modes"}
{"area": "", "auditor": "audit", "body": "## Resolution\n`collectRecipients` always returned a nil error, leaving an unreachable,\nuntestable `if err != nil` branch in `Encrypt`'s RunE. Dropped the error return and\nthe dead branch. Also made the GitHub fetch URL injectable (`githubKeysURL`) so the\nnetwork paths (200/404/connection error) are testable, removing the dead\n\"is-this-really-a-github-URL\" self-check on a URL the code constructs itself.\n\n### Pass 4 — 2026-06-28\n\nPonytail lens (over-engineering hunt).\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-018", "resolved": "2026-06-26", "severity": "", "status": "fixed", "title": "collectRecipients had a vestigial error return"}
{"area": "", "auditor": "audit", "body": "## Resolution\n`ConfigCmd(cfg any, saveConfig func(any) error)` erased types that live in the\nsame package as ConfigCmd — there is no import cycle to dodge — forcing a runtime\n`cfg.(*Config)` assertion, a dead \"internal error\" branch, and a `c.(*cmd.Config)`\nround-trip in a.go. Changed to `ConfigCmd(cfg *Config, saveConfig func(*Config) error)`.\nA type mismatch is now a compile error, so the assertion, the error branch, the a.go\nclosure, and `TestConfigCmd_BadConfigType` were all deleted (net code removed).\n\n### Pass 5 — 2026-06-28\n\nPonytail simplification sweep (5 iterations, race-tested each round).\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-019", "resolved": "2026-06-28", "severity": "", "status": "fixed", "title": "ConfigCmd used `any` parameters for same-package types"}
{"area": "", "auditor": "audit", "body": "## Resolution\n`yamlfmt -c .yamlfmt.yml .` is invalid (`-c` is not a yamlfmt flag), so\n`just format` aborted at that step with a usage dump. Changed to `-conf`. Now both\n`just format` and `just lint` run clean.\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-020", "resolved": "2026-06-28", "severity": "", "status": "fixed", "title": "Justfile `format` target used the wrong yamlfmt flag"}
{"area": "", "auditor": "audit", "body": "## Resolution\nNet deletions, behavior unchanged (race + full suite green each round):\n(1) Decrypt's \"provided key\" branch duplicated the log+tryDecrypt logic already in\n`tryAllKeys`; collapsed it to a one-element slice through `tryAllKeys`.\n(2) Extracted `requireInputOutput`, the input/output flag validation duplicated\nverbatim in Encrypt and Decrypt RunE — single source of truth.\n(3) `parseKeyLines` now uses `strings.SplitSeq` (iterator, no slice alloc) per the\nlinter; removed a needless temp var in `runAgeEncrypt` and a stale \"import Config\nfrom main\" comment.\n(4) Removed dead test scaffolding: the `writeTestResult` helper dumped unread files\ninto temp dirs (7 call sites), plus a redundant `os.Stat` and a pointless\n`HasPrefix` assertion in the roundtrip test; dropped two now-unused imports.\n\n### Pass 6 — 2026-06-28\n\nDeep simplify + coverage loops (10 iterations, race-tested each round).\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-021", "resolved": "2026-06-28", "severity": "", "status": "fixed", "title": "Simplification sweep across code and tests"}
{"area": "", "auditor": "audit", "body": "## Resolution\nPushed coverage from ~88% to ~93% by testing the FS-error branches with\nroot-independent fault injection — ENOTDIR (a path under a regular file) and EISDIR\n(a directory where a file is expected): `InitConfigPaths` mkdir failures and the\n`UserConfigDir` error, `LoadConfig` stat/read/defaults errors, `applyConfigDefaults`\nmkdir error, `readKeyCache` unreadable-cache, `fetchGitHubKeys` body-read error (via\na hijacked short HTTP response), `Encrypt`'s age-failure path, and the two main-pkg\nwrapper error paths. Residual uncovered: `main()` (integration-covered), the darwin\nbranch (GOOS-gated), and a handful of unreachable errors (`yaml.Marshal` of a fixed\nstruct, `UserCacheDir` while config succeeds, a `Body.Close` defer).\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-022", "resolved": "2026-06-28", "severity": "", "status": "fixed", "title": "Untested error branches now covered via fault injection"}
{"area": "", "auditor": "audit", "body": "## Resolution\nThe app used logrus only for JSON logging with levels — exactly what stdlib\n`log/slog` does (Go 1.21+; this module is 1.26). Converted all logging to `slog`,\nremoving the `github.com/sirupsen/logrus` dependency and its transitive\n`golang.org/x/sys`. Direct deps are now cobra, testify, yaml. Also deleted\n`TestSetupLogging`, which tested the logging library rather than our code (our\n`setupLogging` is covered by `TestConfigWrappers`).\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-023", "resolved": "2026-06-28", "severity": "", "status": "fixed", "title": "Replaced logrus with stdlib log/slog"}
{"area": "", "auditor": "audit", "body": "## Resolution\n`ConfigPaths.ConfigDir` was set by `InitConfigPaths` but never read in\nproduction (only two test assertions used it). Removed the field; tests now assert on\n`filepath.Dir(paths.ConfigFile)`.\n\n### Pass 7 — 2026-06-28\n\nLoops 11–15: the final deeper simplification round (race-tested each).\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-024", "resolved": "2026-06-28", "severity": "", "status": "fixed", "title": "Removed dead ConfigPaths.ConfigDir field"}
{"area": "", "auditor": "audit", "body": "## Resolution\nFive iterations, behavior unchanged, coverage steady (~93%):\n(11) `collectRecipients` builds its recipient list with `slices.Concat` instead of a\ntwo-step manual `append`.\n(12) Inlined the single-use `defaultConfig()` constructor at its one call site (the\nbootstrap default is still verified by `TestInitConfigPaths_Full`).\n(13) `tryAllKeys` now returns `(tried []string, ok bool)` instead of mutating a\n`*[]string` out-param — idiomatic Go; the caller drops its pre-declared accumulator.\n(14) Deleted three pure-`NotNil` smoke tests (`TestCmdEncryptPlaceholder`,\n`TestCmdDecryptPlaceholder`, `TestCmdCompletion`) redundant with the functional and\ncmd-package tests; removed the now-unused cobra import from the main test file.\n(15) Hoisted the GitHub-username regex to a package-level `githubUsernameRE` so it is\ncompiled once rather than on every `collectRecipients` call.\n\n### Pass 8 — 2026-07-01\n\nEmpirical end-to-end re-validation (real `age` v1.3.1 binary, isolated `HOME` +\n`XDG_*`) plus a usage improvement.\n\nRe-validation (all hold): full `config`→`encrypt`→`decrypt` round trip recovers\nplaintext (N-002/N-003); file recipients encrypt via `-R` (N-004); decrypted\noutput is `0600` (N-006); first-run `cache_ttl_minutes` materializes as `120`\n(N-012); a failed decrypt with a wrong key leaves no stale output file and the\nsubsequent correct key succeeds, so multi-key scanning is safe. No regressions,\nno new correctness defects.\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-025", "resolved": "2026-06-28", "severity": "", "status": "fixed", "title": "Deeper simplification: stdlib idioms, inlining, idiomatic signatures"}
{"area": "", "auditor": "audit", "body": "## Resolution\n`a config ...` saved silently (no output, no confirmation) and there was no\ncommand to inspect current settings — a typo'd path failed only later at\nencrypt/decrypt time. `ConfigCmd` now marshals the resulting config and echoes it\n(`Configuration saved:\\n<yaml>`), so a save confirms exactly what was written and\n`a config` with no flags doubles as a viewer. Covered by\n`TestConfigCmd_EchoesResultingConfig`. README rewritten with one complete\nsetup→encrypt→decrypt walkthrough and a corrected XDG-aware config path.\n\n### Pass 9 — 2026-07-02\n\nAdversarial security scan (secrets-leak focus) of the library-based codebase.\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-026", "resolved": "2026-07-01", "severity": "", "status": "fixed", "title": "`config` command gave no feedback and had no way to view settings"}
{"area": "", "auditor": "audit", "body": "## Resolution\nCRITICAL. `tryDecrypt` wrote decrypted plaintext straight to the output via\n`os.OpenFile(..., O_CREATE|O_WRONLY|O_TRUNC, 0o600)` and only `chmod`ed to 0600\n*after* `io.Copy`. age authenticates its stream incrementally (64 KiB chunks), so a\ntampered/truncated ciphertext — or any mid-stream failure — wrote one or more\nplaintext chunks and then errored, skipping the chmod. Proven end to end: decrypting\na truncated 198 KB ciphertext onto a pre-existing `0644` file exited non-zero yet\nleft 65536 bytes of plaintext on disk at mode **0664 (group/world-readable)** and\ndestroyed the pre-existing file. Because `tryAllKeys` reuses one output path, a\nheader-matching-but-corrupt attempt could also leave fragments there. Fix: decrypt\nto a temp file created 0600 (`os.CreateTemp`) in the target directory and\n`os.Rename` onto the output only after a fully successful decrypt; remove the temp on\nany failure. Plaintext now never reaches the target path — nor any looser-than-0600\nmode — on a failed or partial decrypt, and a pre-existing file is untouched unless\ndecryption succeeds (rename also replaces rather than follows a symlink at the\ntarget). Covered by `TestTryDecrypt_FailureLeavesNoPlaintext`; full round trip and\n0600 output re-verified.\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-027", "resolved": "2026-07-02", "severity": "", "status": "fixed", "title": "Failed decrypt leaked group/world-readable plaintext to disk"}
{"area": "", "auditor": "audit", "body": "## Resolution\n`encryptFile` opened output with `O_CREATE|O_WRONLY|O_TRUNC` and wrote\nincrementally, so a mid-stream failure truncated any pre-existing file at that path\nand left a partial `.age`. Not a secrets leak (the output is ciphertext, and a\ntruncated age file fails its integrity check on decrypt), but data-loss-prone.\nMirrored the N-027 fix: encrypt to a 0600 temp file in the target dir and\n`os.Rename` onto output only after `age.Encrypt`/copy/close all succeed; remove the\ntemp on any failure. Covered by `TestEncryptFile_FailureLeavesPreexistingIntact`\n(a failed encrypt leaves the pre-existing file intact and no temp residue).\n\n### Pass 10 — 2026-07-02\n\nWhole-repo adversarial sweep (code, docs, infra). govulncheck: no vulnerabilities;\ngitleaks: no leaks across 131 commits.\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-028", "resolved": "2026-07-02", "severity": "", "status": "fixed", "title": "encryptFile was not atomic (clobber/partial output on failure)"}
{"area": "", "auditor": "audit", "body": "## Resolution\n`.goreleaser.yml` set `-X github.com/ivuorinen/a/cmd.version` (and .commit/\n.date/.builtBy) — symbols that do not exist in package `cmd`; the linker silently\nignores `-X` on missing symbols. The real `version` was a `const` in `main`, which\n`-X` also cannot set. Proven: `go build -ldflags \"-X ...cmd.version=9.9.9\"` and\n`-X main.version=9.9.9` both still printed `v0.3.0`, so every release would misreport\nits version. Fix: made `version` a package-level `var` in `main` and pointed the\nsingle ldflag at `-X main.version={{.Version}}`; dropped the three unused\ncommit/date/builtBy flags. Verified `-X main.version=v1.2.3` now yields `v1.2.3`.\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-029", "resolved": "2026-07-02", "severity": "", "status": "fixed", "title": "Release version injection was a no-op"}
{"area": "", "auditor": "audit", "body": "## Resolution\nthe goreleaser brew `test` block ran `system \"#{bin}/a\", \"version\"`, but there\nis no `version` subcommand — cobra exposes only the `--version` flag, so `a version`\nexits non-zero (\"unknown command\"). The formula test would fail on every release.\nProven by running `a version`. Fix: changed the test to `a --version`.\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-030", "resolved": "2026-07-02", "severity": "", "status": "fixed", "title": "Homebrew smoke test invoked a nonexistent subcommand"}
{"area": "", "auditor": "audit", "body": "## Resolution\n`setupLogging` ran in `PersistentPreRunE` for all commands and returned an\nerror if the log file could not be opened, so a `log_file_path` pointing at an\nunwritable/nonexistent location (or a directory) made every command fail — including\n`config`, the only in-tool way to fix the path. Logging carries no secrets and\nencrypt/decrypt do not depend on it, so it now degrades to stderr with a warning\ninstead of erroring. Test updated (`TestSetupLoggingFallback`).\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-031", "resolved": "2026-07-02", "severity": "", "status": "fixed", "title": "A bad log_file_path bricked every command"}
{"area": "", "auditor": "audit", "body": "## Resolution\n`fetchGitHubKeys` used `http.Get` (no timeout — a slow/hostile server hangs\nthe CLI forever) and `io.ReadAll` with no size cap (a huge/hostile response exhausts\nmemory). Fix: a package `http.Client{Timeout: 30s}` and\n`io.ReadAll(io.LimitReader(body, 1<<20))`. Not a leak, but a reliability/DoS hole in\nthe one network path of a security tool.\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-032", "resolved": "2026-07-02", "severity": "", "status": "fixed", "title": "GitHub key fetch had no timeout and unbounded read"}
{"area": "", "auditor": "audit", "body": "## Resolution\n`SaveConfig` used `os.WriteFile` (O_TRUNC then write), so an interrupted or\ndisk-full `config set` truncated the config, silently losing all settings\n(ssh_key_path, recipients, ...). It also inherited a pre-existing file's mode, which\ncould leave the config not-0600 and then rejected by LoadConfig. Fix: write to a\n0600 temp file in the config dir and `os.Rename` over the target — atomic, always\n0600, original preserved on failure. Covered by `TestLoadAndSaveConfig` (round trip\nthrough LoadConfig, which requires 0600).\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-033", "resolved": "2026-07-02", "severity": "", "status": "fixed", "title": "SaveConfig was not atomic — config loss on partial write"}
{"area": "", "auditor": "audit", "body": "## Resolution\n`.golangci.yml` pinned `run.go: \"1.21\"` while go.mod is `1.26.4` (analysis\nran against the wrong language version), and its header comments referenced an\nunrelated \"f2b\"/\"fail2ban\" project. Fix: set `go: \"1.26\"` and corrected the header.\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-034", "resolved": "2026-07-02", "severity": "", "status": "fixed", "title": "golangci-lint config stale and copied from another project"}
{"area": "", "auditor": "audit", "body": "## Resolution\ngoreleaser writes build artifacts to `dist/`, which was not in `.gitignore`\n(only `coverage*`, `out/`, `a` were), so a `just release` followed by `git add`\ncould commit built binaries. Fix: added `dist/`.\n\n### Pass 11 — 2026-07-02\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-035", "resolved": "2026-07-02", "severity": "", "status": "fixed", "title": "dist/ not gitignored"}
{"area": "", "auditor": "audit", "body": "## Resolution\nAll ~257 assertions across the 8 test files used `assert`, which continues\nafter a failure. When a precondition failed — a setup step (`WriteFile`/`MkdirAll`/\n`Flags().Set`), a value-returning helper (`makeSSHKey`), or an `err` checked right\nbefore its value is used (`got, err := ReadFile; assert.NoError; assert.Equal(got)`)\n— the test kept running and produced nil-panics or confusing secondary failures that\nmasked the real cause. Converted preconditions and err-before-use sites to\n`require.*` (fail-fast) while leaving terminal, independent behavioral checks as\n`assert.*` (so all are reported). Split is now ~128 require / ~129 assert. Also\nfixed a stale `age -r` comment in `TestEncryptCmd_AgeFailure`. Behavior/coverage\nunchanged (85.2%); race + lint clean.\n\n### Pass 12 — 2026-07-02\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-036", "resolved": "2026-07-02", "severity": "", "status": "fixed", "title": "Tests never used testify's require, risking cascade failures"}
{"area": "", "auditor": "audit", "body": "## Resolution\n`zizmor 1.26.1 --persona auditor` reported 10 findings (3 medium, 7 low)\nacross the 4 workflows. Autofixer applied the one available fix (artipacked:\n`persist-credentials: false` on the sync-labels checkout). Fixed the rest manually:\n`excessive-permissions` — pr-lint.yml and sync-labels.yml used top-level\n`permissions: read-all`; narrowed both to `permissions: {}` (each job already\ndeclares its own least-privilege set). `concurrency-limits` — added a\n`concurrency` group (cancel-in-progress) to codeql.yml and stale.yml.\n`undocumented-permissions` — added an explanatory comment to every permission\ngrant in codeql/pr-lint/stale/sync-labels; stale.yml's redundant top-level reads\nwere replaced with `{}` since its single job overrides them. Re-run is clean (\"No\nfindings to report\"); yamllint and actionlint both pass.\n\nMigrated from v1 `nitpicker-findings.md` (Pass 1, 2026-06-26).", "category": "", "found": "2026-06-26", "id": "N-037", "resolved": "2026-07-02", "severity": "", "status": "fixed", "title": "GitHub Actions hardening (zizmor --persona auditor)"}
{"area": "cmd/encrypt.go", "auditor": "audit", "body": "## Problem\n\n`collectRecipients` treats every failure to resolve a requested GitHub user as non-fatal. An invalid username (`cmd/encrypt.go:113-115`), a network error, a non-200 status, or a body-read error (`cmd/encrypt.go:157-175`) each `log.Warn` and return nil. Encryption then proceeds with whatever recipients remain — normally the user's own `default_recipients` — and exits 0.\n\nBecause `log_file_path` defaults to a JSON file on disk, nothing is printed to stderr. The user asks for a file encrypted to a colleague, gets a file that colleague cannot open, and is told nothing.\n\n## Evidence\n\nReproduced against the real binary (HOME/XDG dirs isolated, `default_recipients` set to the user's own key):\n\n```\n$ a encrypt msg.txt this-user-definitely-does-not-exist-999zzz\nexit=0\nmsg.txt.age            # produced, without the requested recipient\n\n$ a encrypt msg.txt 'bad user!'\nexit=0\nmsg.txt.age            # same\n```\n\nNothing reaches the terminal — stderr is empty:\n\n```\nSTDERR=$(a encrypt msg.txt this-user-...-999zzz 2>&1 >/dev/null)\nstderr bytes: 0\n```\n\nThe only record is a log line that contains the contradiction and never surfaces it:\n\n```json\n{\"level\":\"INFO\",\"msg\":\"Encrypting file\",\"input\":\"msg.txt\",\n \"recipients\":[\"/…/.ssh/id_ed25519.pub\"],\n \"githubUser\":\"this-user-definitely-does-not-exist-999zzz\"}\n```\n\n`recipients` does not contain a single key belonging to `githubUser`.\n\n`cmd/fetch_test.go:89-96` asserts this behavior as correct:\n\n```go\ncfg := &Config{DefaultRecipients: []string{\"/a.pub\"}, GitHubUser: \"bad user!\"}\ngot, ghUser := collectRecipients(cfg, []string{\"extra\"}, \"\", log)\nassert.Equal(t, []string{\"/a.pub\", \"extra\"}, got)\n```\n\n## Impact\n\nConfidentiality failure in the opposite direction from the usual one: the file is not readable by the party the user intended. A user who encrypts, sends, and deletes the plaintext has destroyed data the recipient can never recover. The failure is indistinguishable from success at the CLI — exit 0, no output, a `.age` file on disk. A transient GitHub outage silently degrades every `a encrypt <file> <user>` in a script.\n\n## Fix\n\nMake an explicitly requested GitHub user a hard requirement. Change `collectRecipients` to return an error, and in `cmd/encrypt.go`:\n\n```go\nfunc collectRecipients(cfg *Config, recipients []string, ghUserFlag string, log *slog.Logger) ([]string, string, error) {\n\tallRecipients := slices.Concat(cfg.DefaultRecipients, recipients)\n\n\tghUser := ghUserFlag\n\tif ghUser == \"\" && cfg.GitHubUser != \"\" {\n\t\tghUser = cfg.GitHubUser\n\t}\n\tif ghUser == \"\" {\n\t\treturn allRecipients, \"\", nil\n\t}\n\tif !githubUsernameRE.MatchString(ghUser) {\n\t\treturn nil, ghUser, fmt.Errorf(\"invalid GitHub username %q\", ghUser)\n\t}\n\tkeys := fetchGitHubKeys(cfg, ghUser, log)\n\tif len(keys) == 0 {\n\t\treturn nil, ghUser, fmt.Errorf(\"no public keys found for GitHub user %q; refusing to encrypt without the requested recipient\", ghUser)\n\t}\n\treturn append(allRecipients, keys...), ghUser, nil\n}\n```\n\nUpdate the `RunE` call site to propagate the error, and update `cmd/fetch_test.go:89-96` to assert the error rather than the silent-skip result — the current assertion encodes the defect.\n\nIf a best-effort mode is genuinely wanted, it must be opt-in (`--allow-missing-recipients`), never the default.\n\n## Resolution\ncollectRecipients now returns (recipients, ghUser, error) and fails closed: an invalid username, or a requested user whose fetch yields zero keys, is an error instead of a log.Warn. Encrypt's RunE propagates it. Verified live against the rebuilt binary: `a encrypt msg.txt 'bad user!'` -> `Error: invalid GitHub username \"bad user!\"`, exit 1, no ciphertext; `a encrypt msg.txt zzqqxnonexistent12345` (valid format, 404) -> `Error: no public keys found for GitHub user \"zzqqxnonexistent12345\"; refusing to encrypt without the requested recipient`, exit 1, no ciphertext. cmd/fetch_test.go:TestCollectRecipients was rewritten — it previously asserted the silent-skip result as correct — and gained a 404 case asserting the same refusal.\n", "category": "security", "found": "2026-08-25", "id": "audit-a87c0ca0", "resolved": "2026-08-25", "severity": "high", "status": "fixed", "title": "Encrypt silently drops an explicitly requested GitHub recipient"}
{"area": "cmd/encrypt.go", "auditor": "audit", "body": "## Problem\n\nBoth commands finish with `os.Rename(tmpName, output)` — `cmd/encrypt.go:317` and `cmd/decrypt.go:74`. `os.Rename` replaces an existing destination unconditionally. Neither command stats `output` first, and there is no `--force` flag to opt into replacement.\n\nBecause the output path is *derived* by default (`<input>.age` for encrypt, `<input>` minus `.age` for decrypt), the destructive case is the common one: the user types a single filename and the tool picks a target that very often already exists.\n\nThe temp-then-rename pattern was introduced to keep a *failed* run from touching the target (documented at `cmd/decrypt.go:19-25`). It does that correctly. It does nothing about a *successful* run destroying an unrelated file.\n\n## Evidence\n\nReproduced against the real binary.\n\nEncrypt clobbers a pre-existing ciphertext:\n\n```\n$ echo \"OLD-CIPHERTEXT-DO-NOT-LOSE\" > keepme.age\n$ echo \"plain\" > keepme\n$ a encrypt keepme\nexit=0\n$ head -c 20 keepme.age\nage-encryption.org/v\n>>> pre-existing keepme.age was OVERWRITTEN without prompt\n```\n\nDecrypt clobbers a newer plaintext:\n\n```\n$ echo \"ORIGINAL-PLAINTEXT-DO-NOT-LOSE\" > doc.txt\n$ a encrypt doc.txt\n$ echo \"REPLACED-BY-DECRYPT?\" > doc.txt     # user edits the original afterwards\n$ a decrypt doc.txt.age\nexit=0\n$ cat doc.txt\nORIGINAL-PLAINTEXT-DO-NOT-LOSE\n>>> the newer doc.txt was OVERWRITTEN by decrypt without prompt\n```\n\nBoth runs exit 0 and print nothing.\n\n## Impact\n\nUnrecoverable data loss in two everyday flows:\n\n- Re-encrypting a file to a different recipient set destroys the previous `.age` — if the old recipients' keys are gone, that ciphertext is unrecoverable.\n- Decrypting an archived `.age` next to a working copy of the same name silently reverts the working copy. Any edits since the encryption are gone.\n\nThere is no undo, no backup, and no prompt. For a tool whose entire purpose is protecting files, destroying one without asking is the wrong default.\n\n## Fix\n\nRefuse an existing output unless the user opts in. Add a shared guard next to `resolveIO` in `cmd/encrypt.go`:\n\n```go\n// ensureWritableOutput refuses to replace an existing output file unless force\n// is set. Rename (the final step of encryptFile/tryDecrypt) replaces the target\n// unconditionally, so without this check a derived output path silently destroys\n// an unrelated file of the same name.\nfunc ensureWritableOutput(output string, force bool) error {\n\tif force {\n\t\treturn nil\n\t}\n\tif _, err := os.Stat(output); err == nil {\n\t\treturn fmt.Errorf(\"output file %s already exists; pass --force to overwrite\", output)\n\t} else if !errors.Is(err, os.ErrNotExist) {\n\t\treturn fmt.Errorf(\"checking output %s: %w\", output, err)\n\t}\n\treturn nil\n}\n```\n\nRegister `cmd.Flags().BoolP(\"force\", \"f\", false, \"Overwrite the output file if it exists\")` on both `Encrypt` and `Decrypt`, and call the guard right after `resolveIO` in each `RunE`. Note this is a TOCTOU-tolerant convenience check, not a lock — that is acceptable for a single-user CLI, and it converts the common accident into an error.\n\nAdd tests asserting that both commands fail on a pre-existing output and succeed with `--force`, and document `--force` in the README command table.\n\n## Resolution\nAdded ensureWritableOutput (internal/cmd/encrypt.go) and a -f/--force flag on both Encrypt and Decrypt; the guard runs immediately after resolveIO in each RunE. Verified live: re-encrypting over an existing keepme.age -> `Error: output file keepme.age already exists; pass --force to overwrite`, exit 1, the old ciphertext byte-identical; with --force it is replaced. Decrypt side: an edited doc.txt survived `a decrypt doc.txt.age` (exit 1) where it was previously reverted. New tests TestEnsureWritableOutput and TestEncryptCmd_RefusesExistingOutput cover the guard and the whole-command path; README documents --force. The docstring records that this is a Stat-then-Rename convenience check, not a lock.\n", "category": "reliability", "found": "2026-08-25", "id": "audit-4141d06c", "resolved": "2026-08-25", "severity": "high", "status": "fixed", "title": "encrypt and decrypt silently overwrite an existing output file"}
{"area": "cmd/config_shared.go", "auditor": "audit", "body": "## Problem\n\nThree call sites read `HOME` directly and never check for the empty string:\n\n- `cmd/config_shared.go:44` — the darwin config-dir branch\n- `cmd/config_shared.go:122` — `applyConfigDefaults`, deriving the default `log_file_path`\n- `cmd/config_shared.go:167` — `ScanSSHPrivateKeys`\n\n`filepath.Join(\"\", \".state\", \"a\")` is `.state/a`, not an error. So with HOME unset the tool writes its log into `$CWD/.state/a/cli.log` and scans `$CWD/.ssh` for private keys — a *different* directory on every invocation.\n\nElsewhere the same file gets this right: `os.UserConfigDir()` and `os.UserCacheDir()` (`cmd/config_shared.go:46, 68`) both return an error when the environment is unusable, and both errors are propagated. The three `os.Getenv(\"HOME\")` sites bypass that.\n\n## Evidence\n\nReproduced against the real binary with a fresh config dir and `HOME` unset.\n\nThe default log path resolves to a relative path and the directory is created in the CWD:\n\n```\n$ cd /tmp/.../cwd2\n$ env -u HOME XDG_CONFIG_HOME=./fresh-cfg a config show\nssh_key_path: \"\"\ncache_ttl_minutes: 120\nlog_file_path: .state/a/cli.log      # <-- relative\n\n$ find . -mindepth 1 | grep -v fresh-c\n./.state\n./.state/a\n./.state/a/cli.log\n```\n\n`ScanSSHPrivateKeys` reads `.ssh` from the current directory — a key planted in `./.ssh` decrypts:\n\n```\n$ mkdir .ssh && ssh-keygen -t ed25519 -N '' -f .ssh/id_planted -q\n$ env -u HOME a encrypt p.txt -r .ssh/id_planted.pub\n$ env -u HOME a decrypt p.txt.age\n$ cat p.txt\nhello                                 # ./.ssh was scanned\n```\n\nWhen the CWD is not writable, the relative `MkdirAll` fails and *every* command dies in `PersistentPreRunE`:\n\n```\n$ chmod 555 . && env -u HOME a config show\nError: error loading config: mkdir .state: permission denied\n```\n\n## Impact\n\nIn any HOME-less context — systemd units without `User=`, cron, minimal containers, `env -i` wrappers — the tool scatters `.state/a/cli.log` directories through every directory it is run from, and reads private keys from whatever `./.ssh` happens to be in the working directory. Running `a decrypt` inside an untrusted checkout makes the tool parse attacker-supplied private keys.\n\nWith a read-only working directory the same path bricks the CLI entirely, `config` included — the same failure class the code already guards against for a bad `log_file_path` (`a.go:55-61`), reachable here through the default instead.\n\n## Fix\n\nUse `os.UserHomeDir()` and propagate its error, matching the treatment `os.UserConfigDir`/`os.UserCacheDir` already get in this file.\n\n`cmd/config_shared.go:120-130`:\n\n```go\nfunc applyConfigDefaults(cfg *Config) error {\n\tif cfg.LogFilePath != \"\" {\n\t\treturn nil\n\t}\n\thome, err := os.UserHomeDir()\n\tif err != nil {\n\t\treturn fmt.Errorf(\"resolving home directory for the default log path: %w\", err)\n\t}\n\tstateDir := filepath.Join(home, \".state\", \"a\")\n\tif err := os.MkdirAll(stateDir, 0o700); err != nil {\n\t\treturn err\n\t}\n\tcfg.LogFilePath = filepath.Join(stateDir, \"cli.log\")\n\treturn nil\n}\n```\n\n`cmd/config_shared.go:165-171`:\n\n```go\nfunc ScanSSHPrivateKeys() ([]string, error) {\n\thome, err := os.UserHomeDir()\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"resolving home directory to scan for SSH keys: %w\", err)\n\t}\n\tsshDir := filepath.Join(home, \".ssh\")\n\t...\n```\n\n`cmd/config_shared.go:42-50`: same substitution in the darwin branch, returning `ConfigPaths{}, err` on failure.\n\nAdd a test with `t.Setenv(\"HOME\", \"\")` asserting each returns an error rather than a relative path.\n\n## Resolution\nAll three os.Getenv(\"HOME\") sites in internal/cmd/config_shared.go replaced with os.UserHomeDir() and the error propagated: the darwin config-dir branch, applyConfigDefaults, and ScanSSHPrivateKeys. Verified live with HOME unset: `a config show` now fails with `error loading config: resolving home directory for the default log path: $HOME is not defined` instead of writing ./.state/a/cli.log, and no .state or .ssh directory is created in the working directory. The read-only-CWD brick is gone with it — the failure is now an explicit environment error rather than a relative MkdirAll. Docstrings on applyConfigDefaults and ScanSSHPrivateKeys record why os.Getenv is wrong here.\n", "category": "correctness", "found": "2026-08-25", "id": "audit-32d1829b", "resolved": "2026-08-25", "severity": "medium", "status": "fixed", "title": "os.Getenv(\"HOME\") resolves to CWD-relative paths when HOME is unset"}
{"area": ".github/workflows", "auditor": "audit", "body": "## Problem\n\nThe repository has 1116 lines of tests reaching 84.1% statement coverage, and nothing ever runs them automatically.\n\nThe four workflows are `release.yml` (tag-triggered GoReleaser), `codeql.yml` (CodeQL analysis), `pr-lint.yml` (a lint action), and `stale.yml` (issue housekeeping). None invokes `go test`. The `.pre-commit-config.yaml` Go hooks are `go-build-mod`, `go-mod-tidy`, `golangci-lint-mod`, and `go-fmt` — also no test hook. `just test` exists but is a manual, local-only entry point.\n\nA test suite that no gate runs does not protect anything; it records what was true the last time a human remembered to type `go test`.\n\n## Evidence\n\nEvery Go-related step across the workflow set:\n\n```\n$ grep -rn 'go test\\|gotestsum\\|-cover' .github/workflows .pre-commit-config.yaml\n(no matches)\n```\n\n`.github/workflows/release.yml:31-34` sets up Go solely to hand it to GoReleaser; `release --clean` builds and publishes without running tests. A tag push therefore ships a release whose test suite was never executed in CI.\n\nContrast with the local tooling that does exist — `Justfile:28-35`:\n\n```\ntest:\n    go test -v ./...\n\ncoverage:\n    go test -coverprofile=coverage.out ./...\n```\n\nVerified locally that the suite passes and is worth gating on:\n\n```\n$ go test -race ./...\nok  github.com/ivuorinen/a       2.609s  coverage: 57.4% of statements\nok  github.com/ivuorinen/a/cmd   1.103s  coverage: 87.5% of statements\ntotal: 84.1%\n```\n\n## Impact\n\nAny pull request that breaks the suite merges green. Any tag push releases untested binaries to GitHub Releases, ghcr.io, and the deb/rpm/apk feeds. The release pipeline is the more serious half: signing, SBOMs, and cosign attestation all certify the provenance of an artifact whose correctness was never checked.\n\nThe 84.1% coverage figure is actively misleading in this state — it describes a suite that only runs when someone chooses to run it.\n\n## Fix\n\nAdd `.github/workflows/test.yml`, pinned by SHA to match the conventions the other workflows already follow:\n\n```yaml\n---\nname: Test\non:\n  push:\n    branches: [main]\n  pull_request:\n    branches: [main]\n  merge_group:\nconcurrency:\n  group: ${{ github.workflow }}-${{ github.ref }}\n  cancel-in-progress: true\npermissions: {}\njobs:\n  test:\n    name: Test\n    runs-on: ubuntu-latest\n    timeout-minutes: 10\n    permissions:\n      contents: read # check out the repository\n    steps:\n      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n        with:\n          persist-credentials: false\n      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0\n        with:\n          go-version-file: \"go.mod\"\n      - run: go test -race -coverprofile=coverage.out ./...\n```\n\n`ubuntu-latest` provides `ssh-keygen`, which several tests need (see the separate finding on that dependency).\n\nGate the release on it too — add `needs: test` with a reusable-workflow call in `release.yml`, or at minimum a `go test ./...` step before the GoReleaser step, so a tag cannot publish a failing build. Make the check required on `main` in branch protection; a non-required workflow is a report, not a gate.\n\n## Resolution\nAdded .github/workflows/test.yml: `go test -race -coverprofile=coverage.out ./...` on push to main, pull_request, and merge_group, on ubuntu-latest (which ships the ssh-keygen the key-generating tests need), with `permissions: {}` at the top and `contents: read` on the job, both actions SHA-pinned to match the existing workflows. Suite verified green locally at 84.9% statement coverage with -race.\n\nNot done, and needs a repository-settings change no commit can make: marking the Test check required in branch protection on main, and gating release.yml on it. A non-required workflow is a report, not a gate — see the finding body for the `needs: test` suggestion.\n", "category": "tests", "found": "2026-08-25", "id": "audit-c7ffd387", "resolved": "2026-08-25", "severity": "medium", "status": "fixed", "title": "No CI workflow runs the test suite"}
{"area": "Dockerfile", "auditor": "audit", "body": "## Problem\n\nThe five-line `Dockerfile` has no `USER` directive and pins nothing:\n\n```dockerfile\nFROM alpine:latest\nRUN apk --no-cache add ca-certificates\nARG TARGETPLATFORM\nCOPY $TARGETPLATFORM/a /usr/local/bin/\nENTRYPOINT [\"a\"]\n```\n\n`alpine:latest` is a moving tag, so the base layer of `ghcr.io/ivuorinen/a:v1.2.3` is not reproducible — rebuilding the same release tag months later yields a different image. `.goreleaser.yml:206` passes `--pull=true`, which guarantees the drift rather than mitigating it.\n\nWith no `USER`, the container runs `a` as uid 0.\n\n## Evidence\n\n`trivy config .` on the repository:\n\n```\nDockerfile (dockerfile)\nTests: 24 (SUCCESSES: 22, FAILURES: 2)\nFailures: 2 (MEDIUM: 1, HIGH: 1, CRITICAL: 0)\n\nDS-0002 (HIGH): Specify at least 1 USER command in Dockerfile with non-root user as argument\nDS-0001 (MEDIUM): Specify a tag in the 'FROM' statement for image 'alpine'\n  Dockerfile:1  FROM alpine:latest\n```\n\n`.goreleaser.yml:193-207` publishes the image to `ghcr.io/ivuorinen/a` under `{{ .Tag }}`, `v{{ .Major }}`, `v{{ .Major }}.{{ .Minor }}`, and `latest`, so the drifting base ships under version tags that imply immutability.\n\nNote the release pipeline signs checksums with cosign and generates SBOMs (`.goreleaser.yml:92-107`) — but `sboms.artifacts: archive` covers the archives only, not the image, so the unpinned base is not even recorded in an SBOM.\n\n## Impact\n\nRunning as root means a container escape from a file-processing tool starts at uid 0. `a` reads private keys and writes plaintext; it needs no privilege at all.\n\nThe unpinned base breaks reproducibility of a security tool's distribution artifact: two builds of the same git tag produce different images, and there is no recorded digest to audit or roll back to. A compromised or simply broken `alpine:latest` push is inherited by the next release build with no changed line in this repository.\n\n## Fix\n\nPin the base by digest and drop to a non-root user:\n\n```dockerfile\n# Pinned by digest: the image tag is immutable, so a rebuild of a release tag\n# reproduces the same base layer. Renovate updates this line.\nFROM alpine:3.22@sha256:<digest>\nRUN apk --no-cache add ca-certificates\nARG TARGETPLATFORM\nCOPY $TARGETPLATFORM/a /usr/local/bin/\n# a only reads/writes user files; it never needs privilege.\nUSER 65532:65532\nENTRYPOINT [\"a\"]\n```\n\nResolve the digest with `docker buildx imagetools inspect alpine:3.22`. The repo already uses `github>ivuorinen/renovate-config` (`.github/renovate.json`), which keeps pinned Docker digests current — the same discipline the workflows already apply to actions (all four are SHA-pinned).\n\nAlternatively drop Alpine entirely: the binary is built `CGO_ENABLED=0` with `netgo`/`osusergo` (`.goreleaser.yml:62-67`), so `gcr.io/distroless/static` works and is non-root by default.\n\nAdd `trivy config --exit-code 1 --severity HIGH,CRITICAL .` to the lint or test workflow so this cannot regress.\n\n## Resolution\nDockerfile now pins `FROM alpine:3.22@sha256:14358309a308569c32bdc37e2e0e9694be33a9d99e68afb0f5ff33cc1f695dce` (digest resolved from the Docker Hub registry API at fix time) and adds `USER 65532:65532`. `trivy config .` re-run: Dockerfile misconfigurations 0 (was 2 — DS-0001 MEDIUM, DS-0002 HIGH). Comments record why the digest pin matters given .goreleaser.yml's --pull=true, and that Renovate keeps the line current.\n\nDistroless was considered and not taken: the binary is CGO_ENABLED=0 with netgo/osusergo so it would work, but swapping the base is a larger change than the finding called for. The trivy gate suggested in the finding body was left out of the test workflow deliberately — see the note on audit-c7ffd387; add it if you want the check enforced rather than advisory.\n", "category": "security", "found": "2026-08-25", "id": "audit-8bd7b563", "resolved": "2026-08-25", "severity": "medium", "status": "fixed", "title": "Container image runs as root from an unpinned alpine:latest base"}
{"area": ".goreleaser.yml", "auditor": "audit", "body": "## Problem\n\n`.goreleaser.yml:80-83` bundles `docs/*` into every release archive:\n\n```yaml\n    files:\n      - LICENSE.md\n      - README.md\n      - docs/*\n```\n\n`docs/` contains exactly one thing: `docs/audit/`, the nitpicker findings store. So every published tarball carries the project's internal defect log — including, after this audit, `docs/audit/findings/audit/open/*.md`, each of which states an unfixed defect, its reproduction, and its impact.\n\nThe glob was presumably written for user documentation that does not exist in this repository.\n\n## Evidence\n\nThe last snapshot build, unpacked:\n\n```\n$ tar tzf dist/a_1.0.1-next_linux_amd64.tar.gz\nLICENSE.md\nREADME.md\ndocs/audit/nitpicker-findings.md\na\n```\n\n`docs/audit/nitpicker-findings.md` is the v1 findings document — 462 lines describing 37 defects, among them entries like \"CLI panics at startup on every invocation\" and a Critical plaintext-leak on failed decryption. All were fixed, but the file also names the exact code paths and failure conditions.\n\nAfter this run the store contains open findings as well, and `docs/*` will pick up `docs/audit/findings/` wholesale. The archive is built for all five `goos` targets, and the same `docs/*` list feeds the `dist/` artifacts confirmed above.\n\n## Impact\n\nEvery release of a security tool ships a curated list of its own weaknesses, with reproduction steps, to anyone who downloads a tarball. For fixed findings this is merely embarrassing and confusing — a user reading the archive cannot tell which entries apply to the version in their hands. For open findings it is a published exploit guide against users who have not upgraded.\n\nIt also inverts the disclosure order: the defect log reaches users through the artifact before any advisory does.\n\n## Fix\n\nStop shipping `docs/`. `.goreleaser.yml:80-83`:\n\n```yaml\n    files:\n      - LICENSE.md\n      - README.md\n```\n\n`README.md` is the only user-facing documentation the repository has, and it is already included.\n\nIf user documentation is added later, list it explicitly (`docs/usage/**`) rather than reinstating a glob that sweeps in whatever else lands under `docs/`. The audit store is a working artifact of the repository, not a distributable.\n\nVerify with `goreleaser release --clean --snapshot --skip-publish && tar tzf dist/a_*_linux_amd64.tar.gz` — the listing should contain only `LICENSE.md`, `README.md`, and `a`.\n\n## Resolution\nRemoved `docs/*` from .goreleaser.yml archives.files; archives now carry LICENSE.md, README.md, and the binary only. Left an explicit comment warning against reinstating the glob, since docs/ holds the findings store. The v1 findings document that was actually shipping (docs/audit/nitpicker-findings.md, confirmed present in dist/a_1.0.1-next_linux_amd64.tar.gz) was also git rm'd as part of the migration cleanup.\n\nNot re-verified by a build: confirming the archive contents needs `goreleaser release --clean --snapshot --skip-publish`, which was out of scope for this run. The change is a two-line deletion in the files list.\n", "category": "security", "found": "2026-08-25", "id": "audit-ab92c934", "resolved": "2026-08-25", "severity": "medium", "status": "fixed", "title": "Release archives ship the internal audit findings to every user"}
{"area": ".goreleaser.yml", "auditor": "audit", "body": "## Problem\n\n`.goreleaser.yml:176` sets the nfpm package description to:\n\n```yaml\ndescription: Modern, secure Go-based CLI tool for managing Fail2Ban jails and bans\n```\n\n`a` is an age encryption wrapper. It has nothing to do with Fail2Ban — the string is copy-paste residue from another project. It is not cosmetic: nfpm writes this field into the metadata of every `.deb`, `.rpm`, and `.apk` the release produces, so it is what `apt show a`, `dnf info a`, and `apk info -d a` print.\n\n## Evidence\n\n`.goreleaser.yml:170-177`:\n\n```yaml\nnfpms:\n  - id: a\n    package_name: a\n    vendor: ivuorinen\n    homepage: https://github.com/ivuorinen/a\n    maintainer: ivuorinen\n    description: Modern, secure Go-based CLI tool for managing Fail2Ban jails and bans\n    license: MIT\n```\n\nThe accurate description is one line away, in `.goreleaser.yml:123`:\n\n```yaml\n    A robust command-line interface (CLI) wrapper around the age encryption tool\n```\n\nand in `README.md:3-5`. The `dist/` tree from the last local snapshot build confirms the packages are actually produced: `a_1.0.1-next_linux_amd64.deb`, `.rpm`, `.apk` and the rest across five architectures.\n\n## Impact\n\nEvery Linux package manager describes the tool as Fail2Ban management software. A user searching for or reviewing the package sees a description that matches neither the binary nor the repository, which reads as a supply-chain red flag on a security tool — the one category of software where mismatched provenance metadata should stop an installation.\n\n## Fix\n\n`.goreleaser.yml:176`:\n\n```yaml\n    description: CLI wrapper for age encryption using SSH and GitHub keys\n```\n\nVerify with `goreleaser release --clean --snapshot --skip-publish` followed by `dpkg-deb -I dist/a_*_linux_amd64.deb | grep Description`.\n\n## Resolution\n.goreleaser.yml nfpms description changed from \"Modern, secure Go-based CLI tool for managing Fail2Ban jails and bans\" to \"CLI wrapper for age encryption using SSH and GitHub keys\". Not re-verified by a package build (`dpkg-deb -I` needs a goreleaser snapshot run, out of scope here); the change is a single literal in the nfpms block.\n", "category": "docs", "found": "2026-08-25", "id": "audit-28a492f9", "resolved": "2026-08-25", "severity": "medium", "status": "fixed", "title": "Linux packages ship a description for an unrelated Fail2Ban tool"}
{"area": "cmd/config_shared.go", "auditor": "audit", "body": "## Problem\n\n`LoadConfig` rejects a config file with any group/other permission bit (`cmd/config_shared.go:101-103`). The check itself is right — the file holds key paths and should be 0600.\n\nTwo things are wrong with how it fails. It runs inside `PersistentPreRunE` (`a.go:93`), so it fails *every* subcommand, including `config set` and `config show` — the only in-tool way to inspect or repair configuration. And the error text states the rule without stating the fix.\n\n`a.go:55-61` already documents this exact failure class for `log_file_path`, and deliberately degrades to stderr rather than \"bricking every command (including `config`, the only way to fix the path)\". The same reasoning was never applied here.\n\n## Evidence\n\nReproduced against the real binary with a 0644 config:\n\n```\n$ chmod 644 $XDG_CONFIG_HOME/a/config.yaml\n$ a config show\nError: error loading config: config file /…/config.yaml must not be group/other accessible (perms 0644)\nexit=1\n\n$ a config set github_user octocat\nError: error loading config: config file /…/config.yaml must not be group/other accessible (perms 0644)\nexit=1\n```\n\n`config set` — the documented repair path (`README.md:49`) — fails with the same error. Nothing in the message mentions `chmod`. The user has to read the source or guess.\n\nThis is reachable without user error: a config restored from a tar archive built without `--same-permissions`, copied via a `umask 022` process, or synced through a filesystem that does not preserve modes arrives at 0644.\n\n## Impact\n\nThe tool is unusable until the user independently works out that the fix is `chmod 600` on a path the error message does happen to print. Every recovery route inside the CLI is blocked by the same check. The severity is bounded by the workaround being a single command once known — but a security control that refuses to say how to satisfy it trains users to reach for the least careful fix they can find.\n\n## Fix\n\nName the remedy in the error. `cmd/config_shared.go:100-103`:\n\n```go\n\t// Reject only group/other access; stricter modes such as 0400 are fine.\n\tif perm := info.Mode().Perm(); perm&0o077 != 0 {\n\t\treturn nil, fmt.Errorf(\n\t\t\t\"config file %s is group/other accessible (perms %#o); fix it with: chmod 600 %s\",\n\t\t\tcfgFile, perm, cfgFile)\n\t}\n```\n\nKeep the check hard — the file is a security surface and silently continuing would be worse. Extend `TestLoadConfig_RejectsGroupOtherPerms` (`cmd/config_shared_test.go:31`) to assert the message contains `chmod 600`, so the remedy cannot be dropped in a later edit.\n\n## Resolution\nThe permission error now names the remedy. Verified live with a 0644 config: `a config set github_user octocat` reports `config file <path> is group/other accessible (perms 0644); fix it with: chmod 600 <path>`. The check itself was left hard — the config file is a security surface and continuing past it would be worse — and a comment records why the remedy belongs in the message (the check runs in PersistentPreRunE, so it blocks `config set`, the documented repair path).\n\nNot done: the finding suggested extending TestLoadConfig_RejectsGroupOtherPerms to assert the message contains \"chmod 600\", which would keep the remedy from being dropped later. The test still only asserts that an error occurs.\n", "category": "reliability", "found": "2026-08-25", "id": "audit-7cbb01e2", "resolved": "2026-08-25", "severity": "low", "status": "fixed", "title": "A group-readable config bricks every command, and the error names no remedy"}
{"area": "cmd/decrypt_test.go", "auditor": "audit", "body": "## Problem\n\nTwo test helpers shell out to `ssh-keygen` and treat its absence as a test failure:\n\n- `cmd/decrypt_test.go:110-117` (`makeSSHKey`) — `require.NoError(t, err, string(out))`\n- `a_test.go:120-134` (`generateSSHKeyPair`) — returns the error, which callers `require.NoError`\n\nThe suite already knows the right pattern. `a_test.go:267-272` guards the same dependency:\n\n```go\nfor _, bin := range []string{\"go\", \"ssh-keygen\"} {\n\tif _, err := exec.LookPath(bin); err != nil {\n\t\tt.Skipf(\"%s not available\", bin)\n\t}\n}\n```\n\nThat guard protects `TestCLIIntegration` only. The two helpers above are unguarded, so `TestEncryptDecrypt_Success`, `TestEncryptDecrypt_WrongKey`, and `TestDecryptCmd_ScanPathRoundTrip` hard-fail on any machine or image without OpenSSH client tools.\n\n## Evidence\n\n`cmd/decrypt_test.go:110-117`:\n\n```go\nfunc makeSSHKey(t *testing.T, dir string) (priv, pub string) {\n\tt.Helper()\n\tpriv = filepath.Join(dir, \"id_ed25519\")\n\t// #nosec G204 -- test helper; dir is a test temp dir\n\tout, err := exec.Command(\"ssh-keygen\", \"-t\", \"ed25519\", \"-N\", \"\", \"-f\", priv).CombinedOutput()\n\trequire.NoError(t, err, string(out))\n\treturn priv, priv + \".pub\"\n}\n```\n\nNo `exec.LookPath` check, in contrast to `a_test.go:267-272`.\n\nThe dependency is currently invisible because no CI job runs the suite at all (filed separately). Adding a test workflow surfaces it: a slim container base (`golang:*-alpine` without `openssh-keygen`, distroless, many self-hosted runner images) has no `ssh-keygen`, and these tests fail with a bare `exec: \"ssh-keygen\": executable file not found in $PATH` that names nothing about the real cause.\n\n## Impact\n\nA missing optional build-environment tool reports as a product test failure. On a slim runner the failure mode is three red tests with an error that does not explain itself, which costs a debugging cycle and invites the wrong fix — deleting the tests rather than provisioning the binary.\n\n## Fix\n\nApply the guard the suite already uses. Add a shared helper in `cmd/decrypt_test.go` and mirror it in `a_test.go`:\n\n```go\n// requireSSHKeygen skips the test when ssh-keygen is absent. Key generation is a\n// build-environment dependency, not a property of the code under test, so its\n// absence must not report as a product failure.\nfunc requireSSHKeygen(t *testing.T) {\n\tt.Helper()\n\tif _, err := exec.LookPath(\"ssh-keygen\"); err != nil {\n\t\tt.Skip(\"ssh-keygen not available\")\n\t}\n}\n```\n\nCall it at the top of `makeSSHKey` and `generateSSHKeyPair`.\n\nThe skip is a fallback, not the plan: the test workflow proposed in the CI finding runs on `ubuntu-latest`, which ships `ssh-keygen`, so the covered path stays covered. If the runner image is ever slimmed, add `openssh-client` to the job rather than letting the skip hide the loss of coverage.\n\n## Resolution\nAdded requireSSHKeygen (internal/cmd/decrypt_test.go) and called it from makeSSHKey; generateSSHKeyPair in a_test.go took a *testing.T and gained the same exec.LookPath guard, with its three call sites updated. Docstrings on both record that ssh-keygen is a build-environment dependency and that the skip is a fallback, not the plan — a runner that starts hitting it has silently lost coverage. Suite still green with -race; the tests do not skip on this machine, so the covered paths stay covered.\n", "category": "tests", "found": "2026-08-25", "id": "audit-2260f687", "resolved": "2026-08-25", "severity": "low", "status": "fixed", "title": "Test helpers hard-depend on ssh-keygen without the skip guard the suite already uses"}
{"area": "README.md", "auditor": "audit", "body": "## Problem\n\nThree files state three different Go versions:\n\n- `go.mod:3` — `go 1.27.0` (the real floor; the toolchain enforces it)\n- `README.md:12` — \"Requires Go 1.26+\"\n- `.golangci.yml:11` — `go: \"1.26\"`\n\n`go.mod` has been bumped twice recently (`a1837d2` 1.26.4→1.26.6, `f07d901` 1.26.6→1.27.0) without either follower being updated, so the drift is a live pattern rather than a one-off.\n\n## Evidence\n\n`go.mod:3`:\n\n```\ngo 1.27.0\n```\n\n`README.md:12`:\n\n```\nRequires Go 1.26+ to build. No runtime dependencies.\n```\n\n`.golangci.yml:8-11`:\n\n```yaml\nrun:\n  timeout: 5m\n  modules-download-mode: readonly\n  go: \"1.26\"\n```\n\nThe README claim is false: a `go 1.27.0` directive makes Go 1.26 refuse the build outright rather than degrade.\n\nThe `.golangci.yml` pin is the more consequential half — `run.go` tells golangci-lint which language version to analyze against, so analyzers reason about 1.26 semantics while the code compiles under 1.27. `usestdlibvars`, `staticcheck`, and the version-gated deprecation checks are the ones that silently diverge.\n\nBoth workflows that set up Go use `go-version-file: \"go.mod\"` (`release.yml:34`, and the proposed test job), so CI itself is correct — only the two hand-maintained copies drift.\n\n## Impact\n\nA contributor on Go 1.26 follows the README, gets a toolchain error the README says should not happen, and has to diff `go.mod` to find out why. golangci-lint analyzing against the wrong language version produces findings that do not match the compiler's view — the failure is quiet in both directions: missed diagnostics and spurious ones.\n\n## Fix\n\n`README.md:12`:\n\n```\nRequires Go 1.27+ to build. No runtime dependencies.\n```\n\n`.golangci.yml:11`: drop the pin entirely rather than restating it. Omitting `run.go` makes golangci-lint read the version from `go.mod`, which removes the file that has to be remembered:\n\n```yaml\nrun:\n  timeout: 5m\n  modules-download-mode: readonly\n```\n\nThat closes the `.golangci.yml` half permanently. The README line still needs a human on each bump; `github>ivuorinen/renovate-config` already opens the `go.mod` PRs, so the README edit belongs in that same PR.\n\n## Resolution\nREADME.md now says \"Requires Go 1.27+ to build\", matching go.mod. The `go: \"1.26\"` pin was deleted from .golangci.yml rather than corrected — with no `run.go` key golangci-lint reads the version from go.mod, which removes the file that has to be remembered on every bump. A comment records why the literal is absent. Only the README line still needs a human on each go.mod bump; the renovate config already opens those PRs.\n", "category": "conventions", "found": "2026-08-25", "id": "audit-6fdb7e9d", "resolved": "2026-08-25", "severity": "low", "status": "fixed", "title": "Declared Go version drifts across go.mod, README, and .golangci.yml"}
{"area": "revive.toml", "auditor": "audit", "body": "## Problem\n\nTwo lint configs at the repo root are never loaded by anything.\n\n`revive.toml` (78 lines) configures ~45 revive rules. revive runs in this project only as a golangci-lint linter (`.golangci.yml:30`), and golangci-lint reads its revive settings from `linters.settings.revive` in `.golangci.yml` — it does not read `revive.toml`. There is no `linters.settings.revive` block, so revive runs with golangci-lint's defaults and every rule in `revive.toml` is inert. Nothing in `Justfile` or `.pre-commit-config.yaml` invokes `revive` standalone.\n\n`.mega-linter.yml` (27 lines) configures MegaLinter. No workflow invokes MegaLinter. It also points at a file that does not exist:\n\n```yaml\nJAVASCRIPT_ES_CONFIG_FILE: .eslintrc.json\nTYPESCRIPT_ES_CONFIG_FILE: .eslintrc.json\n```\n\nThere is no `.eslintrc.json`, and no JavaScript or TypeScript, in this repository.\n\n## Evidence\n\nNo consumer for either file:\n\n```\n$ grep -rn 'revive\\.toml\\|mega-linter\\|megalinter' Justfile .pre-commit-config.yaml .github/ .golangci.yml\n(no matches)\n```\n\n`.golangci.yml:30` enables revive but supplies it no settings:\n\n```yaml\n    - revive # Code style checking\n```\n\nand the `settings:` block (`.golangci.yml:79-113`) configures `errcheck`, `govet`, `gocyclo`, `misspell`, `prealloc`, `errorlint`, and `lll` — no `revive` key.\n\n`revive.toml` documents the coupling it does not have, e.g. `revive.toml:66-77`:\n\n```toml\n# [rule.line-length-limit] # Disabled - conflicts with golangci-lint lll (which is also disabled)\n```\n\n`lll` is in fact *enabled* at `.golangci.yml:31`, so the comment contradicts the config it claims to coordinate with — the file has drifted far enough that its own annotations are wrong.\n\n`.eslintrc.json` is absent:\n\n```\n$ ls .eslintrc.json\nls: cannot access '.eslintrc.json': No such file or directory\n```\n\n## Impact\n\nBoth files read as active policy. A contributor adding a lint rule edits `revive.toml`, sees no change in `golangci-lint run` output, and cannot tell whether the rule is wrong or the file is ignored. The stale comments make it worse by asserting a coordination with `.golangci.yml` that does not exist and is factually inverted. `.mega-linter.yml` implies a lint layer in CI that is not there, overstating the project's actual checks.\n\n## Fix\n\nDelete both files:\n\n```bash\ngit rm revive.toml .mega-linter.yml\n```\n\nIf the revive rules are wanted, port them into `.golangci.yml` where golangci-lint will read them, instead of keeping a second file:\n\n```yaml\n  settings:\n    revive:\n      severity: warning\n      confidence: 0.8\n      rules:\n        - name: exported\n        - name: package-comments\n        - name: early-return\n        # …the rules from revive.toml worth keeping\n```\n\nVerify with `golangci-lint run -v` that revive reports under the ported rules. Deleting is the better default — `.golangci.yml` already enables 27 linters, and the ~45 revive rules were never actually in force, so porting them wholesale would introduce a large batch of new findings that the project has never had to satisfy.\n\n## Resolution\n`git rm revive.toml .mega-linter.yml`. Deleted rather than ported: the ~45 revive rules were never in force (golangci-lint reads linters.settings.revive, not revive.toml), so porting them wholesale would have introduced a large batch of findings the project has never had to satisfy — and .golangci.yml already enables 27 linters. Grepped the tree afterwards for `revive.toml`, `mega-linter`, and `megalinter`: no remaining references in any workflow, Justfile, pre-commit config, or doc.\n", "category": "maintainability", "found": "2026-08-25", "id": "audit-aa41d5f4", "resolved": "2026-08-25", "severity": "low", "status": "fixed", "title": "revive.toml and .mega-linter.yml are dead configuration no tool reads"}
{"area": "cmd", "auditor": "audit", "body": "## Problem\n\nThe module is tagged `v1.0.0` and `cmd/` sits at the module root rather than under `internal/`, so `github.com/ivuorinen/a/cmd` is importable by any third party. Ten symbols are exported, all of them wiring for this one binary.\n\nUnder Go's compatibility convention, a v1 module's exported surface is a promise: changing any of these signatures is a breaking change requiring v2. Nothing in the repository indicates that promise was intended — there is no library documentation, the README describes only the CLI, and every exported function takes or returns cobra and slog types that exist to assemble this program's commands.\n\nSeveral fixes proposed elsewhere in this audit touch that surface. `ScanSSHPrivateKeys() ([]string, error)` keeps its signature, but `LoadConfig` gains a new error condition and `Config` would gain a field if `--force` were persisted — changes that are trivial internally and breaking if the package is public.\n\n## Evidence\n\n```\n$ go doc -short ./cmd\nfunc Completion(rootCmd *cobra.Command) *cobra.Command\nfunc ConfigCmd(cfg *Config, saveConfig func(*Config) error) *cobra.Command\nfunc Decrypt(cfg *Config, log *slog.Logger) *cobra.Command\nfunc Encrypt(cfg *Config, log *slog.Logger) *cobra.Command\nfunc SaveConfig(cfgFile string, cfg *Config) (err error)\nfunc ScanSSHPrivateKeys() ([]string, error)\ntype Config struct{ ... }\n    func LoadConfig(cfgFile string) (*Config, error)\ntype ConfigPaths struct{ ... }\n    func InitConfigPaths() (ConfigPaths, error)\n```\n\n```\n$ git tag --sort=-v:refname | head -1\nv1.0.0\n```\n\nThe package's own doc comment describes it as internal wiring — `cmd/completion.go:1`:\n\n```go\n// Package cmd provides CLI command constructors for the age wrapper.\n```\n\n`README.md` documents commands and configuration keys only; there is no API section, no usage example importing the package, and no godoc link.\n\nThe signatures confirm the intent. `ConfigCmd(cfg *Config, saveConfig func(*Config) error)` takes a save callback because `main` owns the config path — a shape that makes sense for one caller in one program and none for a library.\n\n## Impact\n\nThe project has accepted a compatibility obligation it did not choose and does not track. Routine refactors of CLI plumbing are technically breaking changes; either the maintainer discovers this at the worst moment, or — more likely — makes them anyway and silently breaks any importer while the version number claims stability. `pkg.go.dev` already publishes this surface as v1 API for a module whose README offers no library contract.\n\n## Fix\n\nMove the package under `internal/`, which makes the compiler enforce what the doc comment already says:\n\n```bash\ngit mv cmd internal/cmd\n```\n\nThen update the single import in `a.go:11`:\n\n```go\n\t\"github.com/ivuorinen/a/internal/cmd\"\n```\n\nNo other file imports it, and `go build ./... && go test ./...` verifies the move.\n\nThis is technically a removal of exported API, so do it in the next minor at the latest and note it in the release notes — but the surface was never documented or advertised, so the practical breakage risk is close to zero and shrinks with every release it stays public.\n\nIf a library surface *is* wanted, that is the opposite change and a larger one: it needs a package that does not traffic in `*cobra.Command`, its own documentation, and a deliberate decision to version it.\n\n## Resolution\n`git mv cmd internal/cmd`; the single import in a.go and the one in a_test.go updated to github.com/ivuorinen/a/internal/cmd, plus a stale \"Add subcommands from cmd/*\" comment. The compiler now enforces what the package doc comment already said. `go build ./... && go vet ./... && go test -race ./...` all clean afterwards; coverage 84.9%.\n\nThis removes exported API from a v1 module, so it belongs in release notes as a breaking change on paper. The surface was never documented or advertised and nothing outside the module imported it, so the practical risk is minimal — but the note is the maintainer's call to make, not something this fix can do.\n", "category": "maintainability", "found": "2026-08-25", "id": "audit-9d8979ac", "resolved": "2026-08-25", "severity": "low", "status": "fixed", "title": "cmd/ is an importable package, making CLI internals a semver-bound v1 API"}
{"area": "a.go", "auditor": "audit", "body": "## Problem\n\n`a.go:62-80` has signature `func setupLogging(verbose bool) error` and two `return nil` statements. There is no path that returns non-nil.\n\nThe nil-only return is deliberate — the docstring at `a.go:55-61` explains that a bad `log_file_path` must degrade to stderr rather than brick every command. The signature contradicts that decision: it tells the caller a failure is possible, and `a.go:96` dutifully propagates it as `PersistentPreRunE`'s result, wiring up error handling for a case that cannot occur.\n\nA later edit that adds a real failure inside `setupLogging` will find a plausible-looking `return err` slot waiting for it and reintroduce the exact bricking the docstring rules out.\n\n## Evidence\n\n`a.go:62-80`, both exits:\n\n```go\nfunc setupLogging(verbose bool) error {\n\t...\n\tlogFile, err := os.OpenFile(cfg.LogFilePath, os.O_CREATE|os.O_WRONLY|os.O_APPEND, 0o600)\n\tif err != nil {\n\t\t*log = *slog.New(slog.NewJSONHandler(os.Stderr, opts))\n\t\tlog.Warn(\"could not open log file; logging to stderr\", \"path\", cfg.LogFilePath, \"error\", err)\n\t\treturn nil\n\t}\n\t*log = *slog.New(slog.NewJSONHandler(logFile, opts))\n\treturn nil\n}\n```\n\nThe docstring one line above states the invariant the signature denies — `a.go:60-61`:\n\n```\n// every command (including `config`, the only way to fix the path). It never\n// returns an error.\n```\n\nCall site, `a.go:89-97`:\n\n```go\n\t\tPersistentPreRunE: func(_ *cobra.Command, _ []string) error {\n\t\t\t...\n\t\t\treturn setupLogging(verbose)\n\t\t},\n```\n\n`TestSetupLoggingFallback` (`a_test.go:238`) exercises the fallback branch and cannot assert anything about the error, because there is none to assert.\n\n## Impact\n\nLow and contained — the code is correct today. The cost is that the signature misstates the contract in a place where the contract is the point: the docstring exists specifically because an earlier defect bricked every command through this path. The type should carry that guarantee rather than leaving it to a comment a refactor can ignore.\n\n## Fix\n\nDrop the return value so the compiler enforces what the docstring promises. `a.go:62`:\n\n```go\nfunc setupLogging(verbose bool) {\n```\n\nDelete both `return nil` statements (the fallback branch keeps its `return` to exit early), and change the call site to `a.go:89-97`:\n\n```go\n\t\tPersistentPreRunE: func(_ *cobra.Command, _ []string) error {\n\t\t\tif err := initConfigPaths(); err != nil {\n\t\t\t\treturn fmt.Errorf(\"error initializing paths: %w\", err)\n\t\t\t}\n\t\t\tif err := loadConfig(); err != nil {\n\t\t\t\treturn fmt.Errorf(\"error loading config: %w\", err)\n\t\t\t}\n\t\t\tsetupLogging(verbose)\n\t\t\treturn nil\n\t\t},\n```\n\nNow a future edit that wants to fail from `setupLogging` has to change the signature, which forces the \"should this brick every command?\" question back into review instead of letting it slip through as a `return err`.\n\n## Resolution\nsetupLogging is now `func setupLogging(verbose bool)`; both `return nil` statements became a bare `return` / nothing, and the PersistentPreRunE call site calls it then returns nil explicitly. The docstring states that the absence of an error return is deliberate, so a later edit has to change the signature to fail from here — which forces the \"should this brick every command?\" question back into review. a_test.go's two assert.NoError(t, setupLogging(...)) calls became plain calls; TestSetupLoggingFallback now documents that it asserts non-panic and stderr degradation.\n", "category": "maintainability", "found": "2026-08-25", "id": "audit-c8caf790", "resolved": "2026-08-25", "severity": "low", "status": "fixed", "title": "setupLogging declares an error return it can never produce"}
{"area": "a.go", "auditor": "audit", "body": "## Problem\n\n`a.go:15` hardcodes the fallback version:\n\n```go\nvar version = \"v0.3.0\"\n```\n\nGoReleaser overrides it at release time via `-X main.version={{.Version}}` (`.goreleaser.yml:27`), so tarball and package binaries are correct. Every other build path keeps the literal — `go install github.com/ivuorinen/a@latest` (the first install method the release notes advertise, `.goreleaser.yml:128-131`), `go build`, and `just build`.\n\nThe repository is at tag `v1.0.0`. The fallback has not moved since well before it.\n\n## Evidence\n\n```\n$ git tag --sort=-v:refname | head -1\nv1.0.0\n\n$ go run . --version\na version v0.3.0\n```\n\nThree releases' worth of drift, reported with no indication that the number is a placeholder.\n\nGo has recorded this correctly since 1.18 — the build info embedded by `go install` carries the real module version, and `debug.ReadBuildInfo()` exposes it. The code does not consult it.\n\n## Impact\n\n`a --version` is what a bug report quotes. A user who installed with `go install` reports v0.3.0 while running v1.0.0 code, sending the maintainer to the wrong tag. It also misleads in the safe direction's opposite: a user checking whether they have the release that fixed a security defect sees a version far older than what they hold.\n\n## Fix\n\nRead the version Go already embeds, and keep the ldflags override as the authority for release builds. `a.go:14-15`:\n\n```go\n// version is overridden at release time via -ldflags \"-X main.version=...\".\n// Outside a release build it comes from the module build info, which go install\n// populates with the real module version — a hardcoded fallback silently rots\n// behind the tags, and `a --version` is what bug reports quote.\nvar version = buildVersion()\n\nfunc buildVersion() string {\n\tif info, ok := debug.ReadBuildInfo(); ok && info.Main.Version != \"\" && info.Main.Version != \"(devel)\" {\n\t\treturn info.Main.Version\n\t}\n\treturn \"(devel)\"\n}\n```\n\nAdd `\"runtime/debug\"` to the imports.\n\n`(devel)` is the honest answer for a plain `go build` in a working tree — better than a stale number, because it says outright that the binary is not a release. Verify with `go install github.com/ivuorinen/<email> && a --version`.\n\n## Resolution\nThe hardcoded \"v0.3.0\" is gone; `version` is initialized from a new buildVersion() that reads debug.ReadBuildInfo().Main.Version and falls back to \"(devel)\". The -ldflags \"-X main.version=...\" override in .goreleaser.yml still wins for release builds. Verified on the rebuilt binary: `a --version` now reports `v1.0.1-0.20260825101858-f0fc695fbe98+dirty` — the module pseudo-version for the current dirty tree — instead of v0.3.0 at tag v1.0.0. The docstring records that a stale literal sends bug reports to the wrong tag, which is why the build info is authoritative outside a release.\n", "category": "correctness", "found": "2026-08-25", "id": "audit-d99eb2de", "resolved": "2026-08-25", "severity": "low", "status": "fixed", "title": "go install builds report version v0.3.0 for a v1.0.0 tool"}
{"area": "README.md", "auditor": "audit", "body": "## Problem\n\n`README.md:53-55` lists a Windows config location:\n\n```\nStored at `$XDG_CONFIG_HOME/a/config.yaml` (Linux, …),\n`~/.config/a/config.yaml` (macOS), or `%AppData%\\a\\config.yaml` (Windows)\n```\n\nNo Windows binary is ever produced. `.goreleaser.yml:30-36` builds `linux`, `darwin`, `freebsd`, `openbsd`, `netbsd` — no `windows`. The `format_overrides` entry for `goos: windows` at `.goreleaser.yml:75-77` is dead config for a target that does not exist.\n\nIf it were built, it would not work as documented. `applyConfigDefaults` derives the default log path from `os.Getenv(\"HOME\")` (`cmd/config_shared.go:122`) and `ScanSSHPrivateKeys` does the same (`cmd/config_shared.go:167`). Windows does not set `HOME`, so both collapse to CWD-relative paths — the failure mode proven in the separate `os.Getenv(\"HOME\")` finding, but unconditional rather than edge-case.\n\n## Evidence\n\nNo Windows artifacts in a full snapshot build:\n\n```\n$ ls dist/ | grep -i windows\n(none)\n```\n\n`.goreleaser.yml:30-36` — the complete `goos` list:\n\n```yaml\n    goos:\n      - linux\n      - darwin\n      - freebsd\n      - openbsd\n      - netbsd\n```\n\n`.goreleaser.yml:74-77` — an override for a target that is never built:\n\n```yaml\n    format_overrides:\n      - goos: windows\n        formats: [zip]\n```\n\nConversely, the README omits the three BSD targets that *are* built and shipped.\n\n## Impact\n\nA Windows user follows the README, finds no binary to download, and cannot tell whether the platform is unsupported or the release is broken. Anyone who builds from source on Windows gets a tool that writes its log and searches for SSH keys relative to whatever directory it was launched from, with no error.\n\n## Fix\n\nState the supported platforms accurately. `README.md:53-55`:\n\n```\nStored at `$XDG_CONFIG_HOME/a/config.yaml` (Linux and BSD, default\n`~/.config/a/config.yaml`) or `~/.config/a/config.yaml` (macOS), and created\nwith defaults on first run.\n```\n\nAdd a one-line platform note near Install: `Builds for Linux, macOS, FreeBSD, OpenBSD, and NetBSD. Windows is not supported.`\n\nDelete the dead `format_overrides` block at `.goreleaser.yml:74-77`.\n\nSupporting Windows instead is a larger change than adding `windows` to `goos`: it requires replacing both `os.Getenv(\"HOME\")` sites with `os.UserHomeDir()` (already required by the separate finding on that call), and deciding what `ScanSSHPrivateKeys` should do where `~/.ssh` is not the convention. Do not add the build target without that work.\n\n## Resolution\nREADME.md's config-path line no longer claims a Windows location; it now names Linux/BSD and macOS only, and the Install section states outright: \"Builds for Linux, macOS, FreeBSD, OpenBSD, and NetBSD. Windows is not supported.\" The dead `format_overrides` block for `goos: windows` was deleted from .goreleaser.yml, since no Windows target is built. The README config table also gained a line about the 0600 requirement.\n\nWindows support was not added, per the finding: it would need the os.UserHomeDir migration (done as part of audit-32d1829b) plus a decision about what ScanSSHPrivateKeys should do where ~/.ssh is not the convention. Documenting the actual platforms was the fix.\n", "category": "docs", "found": "2026-08-25", "id": "audit-fcf2b91c", "resolved": "2026-08-25", "severity": "low", "status": "fixed", "title": "Windows is documented as supported but is never built and would not work"}
{"area": "a.go", "auditor": "audit", "body": "## Problem\n\n## Evidence\n\n## Impact\n\n## Fix\n\n## Resolution\n`version` is now a plain uninitialized string so cmd/link's -X can patch it; buildVersion() consults the stamp first and falls back to debug.ReadBuildInfo, then \"(devel)\". Call site uses Version: buildVersion(). The false comment at .goreleaser.yml:23 now records why an initializer breaks -X.\n\nVerified on all three paths: plain `go build -ldflags \"-X main.version=v9.9.9\"` -> \"v9.9.9\" (was the VCS pseudo-version); `goreleaser build --snapshot` logged version=1.0.1-next and the binary reports 1.0.1-next (was v1.0.1-0.2026...+dirty); a no-.git source tree with -X reports 1.0.0 (was \"(devel)\"). TestBuildVersionPrefersLdflagsStamp pins the stamp-wins behavior, since a plain build is rescued by VCS stamping and would pass either way.\n", "category": "correctness", "found": "2026-08-25", "id": "audit-263b545e", "resolved": "2026-08-25", "severity": "high", "status": "fixed", "title": "GoReleaser's -X main.version stamp is silently discarded; releases report a VCS pseudo-version"}
{"area": "internal/cmd/encrypt.go", "auditor": "audit", "body": "## Problem\n\n## Evidence\n\n## Impact\n\n## Fix\n\n## Resolution\nFixed at the root, in resolveIO, rather than in each caller: it now returns the positional args it did not consume, so no caller reads a fixed index that --input can shift. Encrypt takes the github-user from that remainder; both Encrypt and Decrypt reject a leftover arg with \"unexpected argument %q\" instead of dropping it.\n\nVerified end to end on the exact reproduction: `encrypt -i msg.txt definitely-not-a-real-user-zzz9` now exits 1 with \"no public keys found ... refusing to encrypt without the requested recipient\" and creates no file (was exit 0, silent, 219-byte file encrypted to default_recipients only). The positional form `encrypt msg.txt <user>` behaves identically, plain `encrypt -i in -o out` still succeeds, and `decrypt -i a.age -o o.txt /nonexistent/decoy.age` now errors instead of silently decrypting a.age.\n\nThree tests added: TestEncryptCmd_GitHubUserPositionalWithInputFlag, TestEncryptCmd_RejectsUnplaceableArg, TestDecryptCmd_RejectsUnplaceableArg. Full suite passes.\n", "category": "correctness", "found": "2026-08-25", "id": "audit-30cbdbe4", "resolved": "2026-08-25", "severity": "high", "status": "fixed", "title": "encrypt -i <file> <github-user> silently drops the recipient and exits 0"}
{"area": ".mega-linter.yml", "auditor": "audit", "body": "## Problem\n\n## Evidence\n\n## Impact\n\n## Fix\n\n## Resolution\n`git restore --staged --worktree .mega-linter.yml` brought the file back, and only the genuinely dead lines were removed: JAVASCRIPT_ES_CONFIG_FILE and TYPESCRIPT_ES_CONFIG_FILE (both named a .eslintrc.json that does not exist in a repo with no JS/TS), plus JAVASCRIPT_DEFAULT_STYLE, which is dead for the same reason. A header comment now records the coupling the original grep missed -- pr-lint.yml -> ivuorinen/actions/pr-lint -> oxsecurity/megalinter -- and states that ENABLE_LINTERS is an allow-list, so deleting it expands CI linting rather than disabling it.\n\nVerified: the file parses, ENABLE_LINTERS is intact as the four-linter allow-list (YAML_YAMLLINT, MARKDOWN_MARKDOWNLINT, YAML_PRETTIER, JSON_PRETTIER), no ESLint/JavaScript keys remain, and yamllint -c .yamllint.yml passes clean.\n\nrevive.toml deliberately stays deleted. That half of audit-aa41d5f4 was sound: golangci-lint configures revive from linters.settings.revive in .golangci.yml and has no option to load an external revive.toml (confirmed against golangci-lint v2.12.2's reference config), and MegaLinter's GO_REVIVE -- which does declare config_file_name: revive.toml -- stays excluded by the restored allow-list. The ~45 rules were never in force; porting them into .golangci.yml under linters.settings.revive is the deliberate change if they are wanted.\n", "category": "conventions", "found": "2026-08-25", "id": "audit-49db73cf", "resolved": "2026-08-25", "severity": "high", "status": "fixed", "title": ".mega-linter.yml was deleted as dead config, but pr-lint.yml runs MegaLinter through an external action"}
{"area": "internal/cmd/decrypt.go", "auditor": "audit", "body": "## Problem\n\n## Evidence\n\n## Impact\n\n## Fix\n\n## Resolution\nAdded parseSSHIdentity, mirroring filippo.io/age's own cmd/age/parse.go: it detects *ssh.PassphraseMissingError via errors.As, takes the public key the error carries (falling back to <keyPath>.pub for older formats that omit it), and builds an identity with agessh.NewEncryptedSSHIdentity using a term.ReadPassword prompt. tryDecrypt calls it instead of agessh.ParseIdentity.\n\ntryAllKeys was also changed to return the joined failures rather than a bool, because it was swallowing the new diagnosis into the generic \"none of the tried SSH keys matched\" -- fixing the parse without this would have left the user seeing the same wrong message. It returns an explicit error when no key was tried, since errors.Join of nothing is nil and would have read as success.\n\ngolang.org/x/term and golang.org/x/crypto were promoted from indirect to direct (go get); no new module entered the graph.\n\nVerified end to end on a real pty: `a decrypt --ssh-key <passphrase-protected key>` now prompts \"Enter passphrase for ...\", accepts it, and writes the plaintext at mode 0600 -- previously impossible. Without a terminal the error is now \"failed to obtain passphrase: key is passphrase protected and stdin is not a terminal\" instead of \"none of the tried SSH keys matched\". Tests added: makeEncryptedSSHKey helper (the suite only ever generated unencrypted keys, which is why the whole class went uncovered), TestTryDecrypt_PassphraseProtectedKeyIsDiagnosed, TestDecryptCmd_SurfacesPassphraseDiagnosis. go test -race passes.\n", "category": "correctness", "found": "2026-08-25", "id": "audit-81c82cbb", "resolved": "2026-08-25", "severity": "high", "status": "fixed", "title": "Passphrase-protected SSH keys cannot decrypt, and the error blames a key mismatch"}
{"area": "internal/cmd/decrypt.go", "auditor": "audit", "body": "## Problem\n\n## Evidence\n\n## Impact\n\n## Fix\n\n## Resolution\nFixed alongside audit-81c82cbb, since both stemmed from tryAllKeys collapsing every cause into one message. Decrypt now guards len(keys) == 0 before calling tryAllKeys and returns \"no SSH private key to try: ~/.ssh holds no id_* key; pass --ssh-key <path> or set it with: a config set ssh_key_path <path>\" -- naming the fix, per the convention LoadConfig's permission error already established. When keys were tried, the message is \"decryption failed; tried [...]: <joined causes>\", so the actual reason reaches the user instead of a blanket mismatch claim.\n\nVerified: with a ~/.ssh holding only known_hosts, the CLI now prints the remedy above instead of \"none of the tried SSH keys matched / Tried keys: []\". TestDecryptCmd_NoKeysToTry asserts the remedy is named and that the output no longer claims keys were tried; TestTryAllKeys_NoMatch was updated for the new error-returning signature and now also asserts the failure names the key it came from.\n", "category": "reliability", "found": "2026-08-25", "id": "audit-694b4920", "resolved": "2026-08-25", "severity": "low", "status": "fixed", "title": "decrypt reports \"none of the tried SSH keys matched / Tried keys: []\" when it tried no keys at all"}
{"area": ".pre-commit-config.yaml", "auditor": "audit", "body": "## Problem\n\n## Evidence\n\n## Impact\n\n## Fix\n\n## Resolution\nTwo changes, both needed. .pre-commit-config.yaml now overrides the hook's upstream `files: \\.tf$` with `files: ^(Dockerfile|\\.github/workflows/.*\\.ya?ml)$`, the actual IaC surface here; the hook ignores filenames (pass_filenames: false) and always scans `-d .`, so `files` only decides whether it runs. Dockerfile gained `# checkov:skip=CKV_DOCKER_2` above ENTRYPOINT, because a one-shot CLI that runs and exits has no long-running process for a HEALTHCHECK to probe -- without it, enabling the hook would have turned every commit red.\n\nVerified with the project's own runner: `prek run checkov --all-files` now reports \"Checkov ... Passed\" (was \"(no files to check)Skipped\") and `checkov --quiet -d .` exits 0 (was 1). The Dockerfile's 28 other checks and all 92 github_actions checks pass, so the release workflow's elevated token scopes are now actually covered by a gate in the commit path.\n", "category": "security", "found": "2026-08-25", "id": "audit-844d5e41", "resolved": "2026-08-25", "severity": "medium", "status": "fixed", "title": "The checkov pre-commit hook never runs: it is gated on *.tf and the repo has no Terraform"}
{"area": ".goreleaser.yml", "auditor": "audit", "body": "## Problem\n\n## Evidence\n\n## Impact\n\n## Fix\n\n## Resolution\nAdded scripts/gen-notices.sh (a file rather than an inline Justfile recipe, because the go list format string is full of {{...}} that just would try to interpolate) plus a `just notices` recipe. It walks `go list -deps`, so only modules actually linked into the binary appear -- testify and the module itself are correctly excluded -- and emits every LICENSE/LICENSE.txt/LICENSE.md/COPYING/NOTICE it finds. THIRD_PARTY_NOTICES.md is generated and committed; 10 sections covering 9 modules, including both of gopkg.in/yaml.v3's files.\n\nShipped through all three channels: .goreleaser.yml archives.files, nfpms.contents (/usr/share/doc/a/), and dockers_v2.extra_files with a matching COPY in Dockerfile (extra_files is required -- without it the COPY would fail, since the file is not otherwise in the buildx context).\n\nVerified by building the real artifacts with goreleaser (targets trimmed to linux/amd64 for speed, archives/nfpms untouched): the tar.gz now contains LICENSE.md, README.md, THIRD_PARTY_NOTICES.md, a; the .deb contains /usr/share/doc/a/THIRD_PARTY_NOTICES.md. Generation is idempotent across runs. The generated markdown uses ```text fences and folds the filename into the ## heading so it passes the repo's own markdownlint (MD040, and MD024 which the duplicate yaml.v3 heading tripped); shellcheck --severity=warning and shfmt are clean on the script.\n", "category": "maintainability", "found": "2026-08-25", "id": "audit-d98ba877", "resolved": "2026-08-25", "severity": "medium", "status": "fixed", "title": "Binary releases ship no third-party license notices, which BSD-3 and Apache-2.0 dependencies require"}
{"area": "internal/cmd/config_shared.go", "auditor": "audit", "body": "## Problem\n\n## Evidence\n\n## Impact\n\n## Fix\n\n## Resolution\napplyConfigDefaults now resolves the state directory from $XDG_STATE_HOME, falling back to ~/.local/state (the XDG default) instead of the hardcoded, non-standard ~/.state. README documents the default: `$XDG_STATE_HOME/a/cli.log` (`~/.local/state/a/cli.log`).\n\nVerified on the built binary: with XDG_STATE_HOME set, log_file_path is <that dir>/a/cli.log and the directory is created there; with it unset, ~/.local/state/a/cli.log.\n\nThe change surfaced a real test-isolation defect that had to be fixed with it. XDG_STATE_HOME outranks HOME, so the developer's actual XDG_STATE_HOME (/home/ivuorinen/.local/state) leaked into the test process: it defeated errpaths_test.go's fileHome fault injection -- three tests failed outright -- and, worse, pointed the tests' MkdirAll at the real home directory. fileHome now clears XDG_STATE_HOME, and every test that reaches applyConfigDefaults sets it to a temp dir: TestApplyConfigDefaults, TestLoadConfig_MissingReturnsDefaults, TestLoadConfig_AcceptsStricterPerms, TestInitConfigPaths_Full, TestConfigWrappers, TestDefaultLogFilePath, and TestCLIIntegration's subprocess env (which inherits os.Environ()).\n\ngo test -race passes with XDG_STATE_HOME both set and unset, and `~/.local/state/a` is confirmed absent afterwards -- no leak into the real home. TestApplyConfigDefaults now asserts both the default and that XDG_STATE_HOME wins, so ignoring the variable again fails the suite.\n", "category": "conventions", "found": "2026-08-25", "id": "audit-2f6507e1", "resolved": "2026-08-25", "severity": "low", "status": "fixed", "title": "Log defaults to the non-standard ~/.state, ignoring XDG_STATE_HOME, and the location is undocumented"}
{"area": "README.md", "auditor": "audit", "body": "## Problem\n\n## Evidence\n\n## Impact\n\n## Fix\n\n## Resolution\nREADME's Install section now lists every channel the release actually publishes, source build last: prebuilt binaries/tar.gz from the releases page, the .deb/.rpm/.apk packages, the ghcr.io/ivuorinen/a container, `go install`, and building from source. Added a \"Verifying a download\" subsection with the cosign verify-blob invocation, since the release keyless-signs its checksums and nothing told the reader that.\n\nThe container example carries `-u \"$(id -u):$(id -g)\"` and a note that the image runs as uid 65532 -- per the finding, an unqualified `docker run` with a bind mount would fail on first use for most users, which is worse than omitting the example.\n\nAlso documented `--version` alongside `-v` in the Commands section, since a --version that reports the right number is only useful if bug reporters know to quote it (see audit-263b545e).\n\nVerified: markdownlint -c .markdownlint.json '**/*.md' passes clean across the repo, and the documented channels match .goreleaser.yml's archives/nfpms/dockers_v2 blocks.\n", "category": "docs", "found": "2026-08-25", "id": "audit-a1491aed", "resolved": "2026-08-25", "severity": "low", "status": "fixed", "title": "README's Install section omits every distribution channel the release actually publishes"}
{"area": "Dockerfile", "auditor": "audit", "body": "## Problem\n\n## Evidence\n\n## Impact\n\n## Fix\n\n## Resolution\nDockerfile now sets ENV HOME=/home/nonroot and creates that directory owned by 65532 before dropping to USER 65532:65532 (the RUN must precede USER, since chown needs root). The non-root user is unchanged.\n\nVerified by running the image, not just building it. Before: every subcommand died with \"error initializing paths: mkdir /.config: permission denied\", including `completion bash`; only `--version` worked, because cobra answers it before PersistentPreRunE. After: `docker run --rm a completion bash` prints the completion script, and `id -u` is still 65532 with HOME=/home/nonroot.\n\nA second, separate constraint surfaced while verifying and is now documented rather than papered over: with a bind mount owned by the host user, the image's own uid 65532 still cannot write there -- that is Docker's ownership model, not a defect in `a`. The working invocation is `-u \"$(id -u):$(id -g)\" -e HOME=/work`, and a full encrypt+decrypt round-trip through the container was confirmed with it, output owned by the host uid. README now documents exactly that command and explains why both flags are required; the earlier draft, which led with a bare `docker run -v`, was wrong and was replaced.\n\nThe finding's suggested CI smoke test was not added -- release.yml does not currently run the image at all, and adding a container smoke step is a larger change than this finding's scope. Recorded as a follow-up in the run summary.\n", "category": "reliability", "found": "2026-08-25", "id": "audit-6b2d43ee", "resolved": "2026-08-25", "severity": "high", "status": "fixed", "title": "The published container image fails on every command: uid 65532 has HOME=/ and cannot create /.config"}
{"area": ".github/workflows/test.yml", "auditor": "audit", "body": "## Problem\n\n## Evidence\n\n## Impact\n\n## Fix\n\n## Resolution\nAdded a \"Check third-party notices are current\" step to .github/workflows/test.yml, before the test step: it runs scripts/gen-notices.sh and fails if the working tree changed.\n\nThe check uses `git status --porcelain -- THIRD_PARTY_NOTICES.md` rather than the `git diff --exit-code` the finding proposed. Writing it revealed that diff only compares tracked files, so it would silently pass if the file were untracked or removed from git -- the same compliance gap as a stale file, undetected.\n\nVerified in a scratch repo across three cases: (1) committed and current -> PASS; (2) committed but stale -> FAIL, drift detected; (3) file removed from git tracking -> FAIL, drift detected. The original `git diff --exit-code` formulation was confirmed to report PASS for case 3, which is why it was replaced. On failure the step prints the diff and an ::error:: annotation naming `just notices`.\n", "category": "maintainability", "found": "2026-08-25", "id": "audit-66ac0b2a", "resolved": "2026-08-25", "severity": "low", "status": "fixed", "title": "Nothing detects a stale THIRD_PARTY_NOTICES.md after a dependency bump"}
{"area": "internal/cmd/decrypt.go", "auditor": "audit", "body": "## Problem\n\n## Evidence\n\n## Impact\n\n## Fix\n\n## Resolution\nThe prompt is now a package-level `passphrasePrompt` var, following the idiom this package already uses for test injection (githubKeysURL in encrypt.go), so no new dependency and no pty helper. parseSSHIdentity calls it through a closure that supplies keyPath.\n\nTwo tests now cover the path that previously could not be reached: TestTryDecrypt_PassphraseProtectedKeyRoundTrip (encrypted key actually decrypts, content matches, output is 0600) and TestTryDecrypt_WrongPassphrase (a wrong passphrase errors rather than producing garbage). TestTryDecrypt_PassphraseProtectedKeyIsDiagnosed deliberately does not override the prompt, so the no-terminal branch stays covered too.\n\nparseSSHIdentity coverage 50.0% -> 61.5%, package total 84.6% -> 85.7%. The residual uncovered lines are the .pub fallback, which needs a key format that does not embed its public key.\n\nRe-verified the real terminal path after the refactor, since the change moved the code the manual check exercised: driving the built binary under script(1) with a passphrase-protected key still prompts and decrypts (\"tty secret\"). go test -race passes.\n", "category": "tests", "found": "2026-08-25", "id": "audit-fcb6c1a5", "resolved": "2026-08-25", "severity": "medium", "status": "fixed", "title": "The successful passphrase-key decryption path is untestable and untested: the prompt is hardcoded to the terminal"}
{"area": ".golangci.yml", "auditor": "audit", "body": "## Problem\n\n## Evidence\n\n## Impact\n\n## Fix\n\n## Resolution\nAll three issues fixed and the gate is now wired into CI.\n\n1. fetch_test.go:164,169 -- added `// #nosec G304 -- test temp path`, matching the 11 other annotated os.ReadFile sites in tests.\n2. a_test.go generateSSHKeyPair -- now takes only *testing.T and uses t.TempDir() per keypair instead of os.MkdirTemp(dir, \"key\"); the dir parameter is gone and the three call sites updated. The docstring records why t.TempDir(): it registers its own cleanup and needs no parent directory threaded through callers.\n3. .github/workflows/test.yml gained a Lint step using golangci/golangci-lint-action pinned to commit ba0d7d2ec06a0ea1cb5fa41b2e4a3ab91d21278a (v9.3.0, tag object dereferenced to its commit and cross-checked -- a tag-object SHA would not have resolved), version v2.13.1, args --fix=false so CI reports rather than rewriting source via .golangci.yml's issues.fix: true.\n\nThe lens itself is no longer uncovered. Rebuilding the current releases with the local go1.27.0 toolchain resolves the version mismatch that blocked earlier passes: golangci-lint v2.13.1, staticcheck 2026.2.1, gosec v2.28.0, govulncheck v1.7.0 all run. Final state: golangci-lint 0 issues (exit 0), staticcheck 0, gosec 0, govulncheck 0 affecting the code. govulncheck also confirms the single module-level advisory (x/crypto/openpgp) is not called, which earlier passes had only asserted.\n\ngo test -race passes, coverage 85.7%. actionlint, yamllint, markdownlint, shellcheck, checkov and goreleaser check all exit 0.\n", "category": "conventions", "found": "2026-08-25", "id": "audit-a4407a09", "resolved": "2026-08-25", "severity": "low", "status": "fixed", "title": "golangci-lint exits 1 on three pre-existing issues, and no workflow runs it"}
